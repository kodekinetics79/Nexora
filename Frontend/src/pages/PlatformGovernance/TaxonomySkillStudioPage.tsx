import ArtifactStudioPage from './ArtifactStudioPage';

export default function TaxonomySkillStudioPage() {
  return <ArtifactStudioPage
    title="Commercial Taxonomy & Document Skills"
    subtitle="Version customer and supplier document schemas, extraction strategies, validation and downstream actions."
    types={['CommercialTaxonomy', 'DocumentSkill']}
  />;
}
